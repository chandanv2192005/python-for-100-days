#Converting input to numbers:
age = int(input("Enter your age: ")) # converts to integer
height = float(input("Enter height: ")) # converts to float
name = input("Enter your name: ") # stays as string (no conversion needed)